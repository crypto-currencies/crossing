/**
 * @deprecated Removed — do not use.
 *
 * Mock sessions were used during early prototyping and have been replaced by
 * the real auth system (lib/services/auth.service.ts). All session data now
 * comes from the database via /api/auth/session.
 *
 * If you need a test user, run: npx prisma db seed
 */
export {};
