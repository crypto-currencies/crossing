"use client";

/**
 * Placeholder for future real-time presence via WebSocket.
 * No fake events are generated — the live activity feed shows
 * its empty state until real events arrive from the server.
 */
export function useLivePresence(): void {
  // Real-time events will be populated via WebSocket in a future update.
}
